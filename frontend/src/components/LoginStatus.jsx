import React from "react";

/**
 * Authentication has been removed.
 * This component is kept only for compatibility in case older UI still imports it.
 */
export default function LoginStatus() {
  return null;
}
